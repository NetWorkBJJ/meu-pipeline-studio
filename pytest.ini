[pytest]
testpaths = test/python
python_files = test_*.py
python_functions = test_*
addopts = --tb=short --junitxml=test-results/python-results.xml -v
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    bridge: tests that spawn the Python bridge process
